name = "aerbsi832/safetensors-toolkit"

version = "0.1.0"

readme = "README.md"

license = "Apache-2.0"

keywords = [ "safetensors", "model-format", "validation", "moonbit" ]

description = "SafeTensors structural validation, diff, and streaming shard tools for MoonBit"

source = "src"

preferred_target = "native"

import {
  "moonbitlang/async@0.19.1",
  "moonbitlang/x@0.4.48",
}
